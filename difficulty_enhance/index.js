/** @type {import('../../code/index').OutertaleMod} */
/** @type {import('../../code/index').OutertaleMod} */
export default async function (mod, api) {
    // 导入API
    const {
        SAVE,
        mechanics
    } = api;
    //mechanics.overrideHP = 1;
    //SAVE.data.n.hp = 1;
}